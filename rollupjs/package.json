{
  "scripts": {
    "build": "rollup -c ./rollup.config.js",
    "watch": "rollup -c ./rollup.config.js -w"
  },
  "devDependencies": {
    "@stylify/bundler": "^0.0",
    "postcss": "^8.4",
    "rollup": "^2.61",
    "rollup-plugin-postcss": "^4.0"
  }
}
